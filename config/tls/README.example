# TLS 证书目录（示例说明）

本目录存放 TLS/mTLS 证书。**全部证书文件不入 Git**（见根目录 `.gitignore`），clone / pull 后由 `./pull.sh` 或本脚本在本地生成。

## 生成（开发）

```bash
./scripts/gen_tls_certs.sh          # 缺失时生成；已存在则跳过
./scripts/gen_tls_certs.sh --force  # 强制重新签发
```

产物：

| 文件 | 说明 |
|------|------|
| `ca.crt` / `ca.key` | 开发 CA（`ca.key` 保密） |
| `server.crt` / `server.key` | 全区 dev 共用服务端/客户端证书 |

## 配置

- 游戏区：`config/config.xml` → `<Tls cert="..." key="..." ca="..."/>`
- 外联：`LoginServer/extern_login.xml` 等 → 同名 `<Tls>` 段

## 客户端

将 `ca.crt` 安装到 RPG_Client 信任库，或 dev 跳过校验。

详见 `docs/TLS.md`。

默认仅允许 ECDHE + AES-GCM 套件；可在 `config.xml` / `extern_*.xml` 的 `<Tls cipherSuites="..." tls13CipherSuites="..."/>` 覆盖。
