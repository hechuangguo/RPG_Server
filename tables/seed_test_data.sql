-- ============================================================
--  RPG 游戏服务器测试种子数据脚本（仅开发/测试环境）
--  数据库：rpg_game（须先执行 tables/init.sql）
--  说明：
--    1) 本脚本面向 CharBase 合表方案（无独立账号表/用户表）
--    2) binary 字段用于功能数据二进制序列化，示例写入空二进制 x''
--    3) INSERT IGNORE 幂等，可重复执行；生产环境禁止导入
-- ============================================================

USE rpg_game;

-- -----------------------------------------------------------
-- 测试角色基础数据（CharBase）
-- 用途：Scene / Record / Session 联调；map_id=1001 对应新手村
-- binary 当前置空，后续由服务端序列化写入包裹/技能/Buff/任务集合
-- -----------------------------------------------------------
INSERT IGNORE INTO CharBase
    (user_id, name, level, vocation, sex, map_id, pos_x, pos_y, pos_z, hp, max_hp, mp, max_mp, gold, `binary`)
VALUES
    (1, 'test001', 1, 1, 1, 1001, 0, 0, 0, 100, 100, 100, 100, 1000, x''),
    (2, 'test002', 5, 2, 1, 1002, 10, 0, 8, 180, 200, 120, 150, 5000, x''),
    (3, 'test003', 10, 3, 2, 2001, 22, 0, 16, 260, 300, 180, 220, 12000, x'');

-- -------------------------------------------------------
-- 测试社交占位（SessionServer 读 Relation）
-- -------------------------------------------------------
INSERT IGNORE INTO Relation (user_id, friends_json, blacklist_json, guild_id, team_id)
VALUES
    (1, '2,3', '', 0, 0),
    (2, '1',   '', 0, 0),
    (3, '1',   '', 0, 0);
