---
description: 头文件、XML 配置、SQL 脚本及新增代码的注释要求（alwaysApply）
alwaysApply: true
---

# 注释规范（全项目必遵）

本仓库 **所有** `.h` 头文件、`config/*.xml` 运行时配置、`tables/*.sql` 数据库脚本，以及 **C++ / Lua / Shell / CMake** 的新增与修改，必须附带与现有代码风格一致的注释。  
**以后所有新加内容**默认遵循本规则；修改存量文件时，若触及某文件/符号，应同步补齐注释。

## 一、C++ 头文件（`.h`）

| 类型 | 要求 |
|------|------|
| **文件头** | `@file`、`@brief`；复杂模块补充职责、依赖、线程/存档约束 |
| **类 / 结构体 / 枚举** | `@brief`；枚举值用 `/**< ... */` |
| **public 方法** | `@brief`；非显然参数/返回值用 `@param` / `@return` |
| **成员变量** | 非自解释字段用 `/**< ... */`（尤其 dirty、快照、容量上限） |
| **方法排版** | class/struct 内**相邻方法之间空一行**（范本 `SceneServer/SceneUserManager.h`；注释与声明之间不空行） |
| **协议 / 消息** | 方向、触发时机、payload 布局（见 `protocal/InternalMsg.h`） |

自解释的一行 getter 可省略；业务边界、存档语义、魔法数必须写。

## 二、XML 配置（`config/*.xml`）

| 类型 | 要求 |
|------|------|
| **文件头** | `<!-- ... -->` 块：文件名、职责、谁读取、修改是否需重启 |
| **配置段** | 每个主要节点段（如 `<Database>`、`<SuperServer>`、`<LogPaths>`）有用途说明 |
| **关键节点/属性** | 含义、默认值、影响范围（参考 `config/config.xml`） |

## 三、SQL 脚本（`tables/*.sql`）

| 类型 | 要求 |
|------|------|
| **文件头** | `--` 块：脚本用途、库名、执行顺序、是否可重复执行 |
| **每张表** | 表前 `--` 段：设计意图、读写进程、与哪张表关联 |
| **字段** | 优先 `COMMENT '...'`；复杂语义在表头段补充 |
| **种子数据** | 说明环境（dev/test only）、账号/密码约定、幂等策略 |

## 四、其它源码

| 类型 | 要求 |
|------|------|
| **`.cpp`** | 文件头 `@file` / `@brief`；实现非显然分支时加行内或块注释 |
| **Lua / Shell** | 文件头说明；非显然函数与参数 |
| **CMake** | 非显然 target、依赖、宏参数 |

## 注释原则

- **解释 why / 约束**，不要复述代码字面意思
- **与现有风格一致**：C++ 用 Doxygen；XML 用 `<!-- -->`；SQL 用 `--` 与 `COMMENT`
- **注释块内禁止 `*/` 子串**（会提前结束 C++ 块注释）
- 未用注释掩盖应修复的命名或结构设计问题

## 示例

```cpp
/**
 * @brief 向指定槽位放入道具（同槽同模板可堆叠）
 * @param slot   格子索引，从 0 开始
 * @param itemId 物品模板 ID
 * @param count  数量
 * @return 成功 true
 */
bool addItem(BagType bagType, uint16_t slot, uint32_t itemId, uint32_t count);
```

**方法排版**（相邻方法之间空一行，范本 `SceneServer/SceneUserManager.h`）：

```cpp
    /** @brief 按 userId 查找在线用户 */
    std::shared_ptr<SceneUser> findUser(UserID userId) const;

    /** @brief 注册在线用户 */
    bool addUser(UserID userId, std::shared_ptr<SceneUser> user);
```

```xml
<!-- RecordServer 监听端口；仅内网，客户端不可直连 -->
<RecordServer port="9002"/>
```

```sql
-- 表：t_bag — Scene 经 Record 落库（每用户每槽一行）
CREATE TABLE IF NOT EXISTS t_bag (
    slot INT UNSIGNED NOT NULL COMMENT '格子索引，从 0 起',
    ...
);
```

## 检查清单（提交前）

- [ ]  touched 的 `.h` 有文件头 + 对外 API 说明
- [ ]  touched 的 `config/*.xml`、`tables/*.sql` 有关键段/表注释
- [ ]  新对外 API / 协议 / 配置项有说明
- [ ]  非显然分支、魔法数、时序依赖有注释
- [ ]  touched 的 `.h` 中相邻方法声明之间有空行

## 文档索引

- 人类可读细则：[`docs/COMMENTS.md`](docs/COMMENTS.md)
- Agent 速查：[`AGENTS.md`](AGENTS.md) 提交前自检
